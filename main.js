import { linkedList } from "./index.js";

const list = new linkedList;

list.preppend(20)
list.append(10)
list.preppend(30)
list.append(40)
list.toString();
list.size();
list.first();
list.tail();
list.at(5)
list.pop();
list.contains(100)
list.find(300)